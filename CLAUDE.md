# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Commands

### Frontend (Next.js — root directory)
```bash
npm run dev          # Start Next.js on port 3000 (webpack mode)
npm run build        # Production build
npm run lint         # ESLint
npx tsc --noEmit     # The only real type check — see the warning below
```

**`npm run build` does not type-check.** `next.config.ts` sets `typescript.ignoreBuildErrors: true`, so a build succeeds with type errors still in the tree. Run `npx tsc --noEmit` if you want them caught.

### Backend (FastAPI — `api/` directory)
```bash
# From repo root:
npm run dev:backend  # Windows only — this script shells out to `py`

# macOS/Linux (this machine): use the venv interpreter directly
cd api && ../.venv/bin/python -m uvicorn app.main:app --port 8000 --reload
```

`npm run dev:backend` / `npm run dev:all` / `run_dev.bat` all invoke `py`, the **Windows** Python launcher, which does not exist on macOS. On darwin use `.venv/bin/python` as above (or run the two processes in separate terminals).

### Run both together
```bash
npm run dev:all      # Concurrently starts both — Windows only, same `py` caveat
# Or on Windows: run_dev.bat
```

### Backend Python environment
The backend uses a virtualenv at `.venv/` (repo root, not `api/`). Install Python deps from `api/requirements.txt`:
```bash
.venv/bin/pip install -r api/requirements.txt
```

### Tests
**There is no test suite and no test runner** — no pytest, jest, or vitest anywhere. The `test_*.py` files at the root and in `api/` (`test_sync.py`, `api/test_import.py`, `api/test_sync_manual.py`, `check_db_props.py`, `tmp_gen_excel.py`) are one-shot throwaway scripts written to poke a live endpoint or connection, not a suite. Verify changes by running the app against real MEWS/Supabase data and comparing against the reference spreadsheets (see **Ground truth** below), which is how every report in here was built.

### Environment variables
The backend requires a `.env` file inside `api/` with: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `MEWS_CLIENT_TOKEN`, `MEWS_ACCESS_TOKEN`, `MEWS_BASE_URL`, and `ENCRYPTION_KEY`. `APP_BASE_URL` (`api/app/config.py`) defaults to the production URL and is what email links point at — the backend has no `window.location` to fall back on the way the frontend's `getBaseUrl()` does.
The frontend reads `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` from `.env.local` in the root.

---

## Architecture

NHGOne is a hospitality management dashboard for Narai Hospitality Group (8 Lub d properties). It surfaces data from **MEWS** (the PMS — Property Management System) through a sync pipeline into **Supabase** (PostgreSQL), then displays it via a Next.js frontend. A large and growing share of it is **statutory and finance reporting** — Thai Hotel Act forms (ร.ร.๓ / ร.ร.๔), Immigration TM30, and pipe-delimited SunSystems/Infor journal files — where the output has to match an existing hand-maintained spreadsheet or government form exactly.

### Two-process design (local dev)

| Process | Location | Port |
|---------|----------|------|
| Next.js (TypeScript + Tailwind) | `src/` | 3000 |
| FastAPI (Python 3.12) | `api/` | 8000 |

In local dev, `next.config.ts` rewrites `/api/*` → `http://127.0.0.1:8000/*` so the frontend calls the backend with relative paths. In production (Vercel), `vercel.json` rewrites `/api/*` → `api/index.py`, which boots the same FastAPI app via `app.root_path = "/api"`.

### Data flow

```
MEWS API  →  FastAPI SyncService  →  Supabase (PostgreSQL)  →  Next.js frontend
```

1. **MEWS API** — `api/app/services/mews_client.py` wraps all MEWS Connector API v1 calls. Each property has its own `ClientToken`/`AccessToken` stored encrypted in the `property_api_settings` Supabase table.
2. **SyncService** (`api/app/services/sync_service.py`) fetches and maps MEWS data to the 58-column MEWS Reservation Report schema, then upserts to Supabase tables: `reservations_sync`, `members_sync`, `payments`.
3. **Scheduled jobs** — see the next section; there are now many independent schedules, not one.
4. **Encryption** — `EncryptionService` (Fernet/AES) encrypts PII fields (names, emails, phone numbers) before storage. The per-field list is `api/app/services/encryption.py:SENSITIVE_FIELDS`. Reports whose PII is *nested* (guest lists inside a list inside a dict) can't be reached per-field and are instead stored as a **whole-blob** encrypted string — see the report-module pattern below.

### `sync_service.py` is the monolith

`api/app/services/sync_service.py` is ~6,100 lines and holds essentially every MEWS-facing report builder. When looking for logic, grep it before assuming a new file. Its rough order: reservation/member/payment/resource/bill mappers → bill invoice + totals → RR3 cards → RR4/TM30 → ST Files → Occupancy/Rate → RV (revenue journal) → email digests → BCP snapshot. The only deliberate exceptions live outside it: `reconciliation_service.py` (pure Excel-in/Excel-out, no MEWS or Supabase at all), `email_service.py`, `ftp_service.py`, `encryption.py`, `mews_client.py`, and `rr4_tm30_reference.py` (static nationality lookup tables).

### The report-module pattern

ST Files, RV, RR4/TM30 and Occupancy/Revenue are all the same shape, and a new report should follow it rather than inventing a fifth arrangement:

1. **Builder** in `sync_service.py` — `get_<name>_report(property_name, date)`, one live MEWS fetch, returns a plain dict.
2. **Router** at `api/app/routers/<name>.py` with a near-identical endpoint set: `GET /report` (live), `GET /managed` (read the cached row), `POST /sync-manual` (Import To Data Mart over a date range, capped at ~62 days), `GET /list` (which dates have been imported), `GET /export` (the pipe-delimited or `.xlsx` file).
3. **A shared `sync_<name>_day()` helper** in that router, called by *both* the manual import button and the scheduled job — this is deliberate, so the two can never drift apart.
4. **Storage** in a `<name>_sync` table, one row per `(property, report_date)`, upserted `on_conflict="property,report_date"`.
5. **Schedule** — a `daily_auto_sync_<name>()` in `api/app/main.py` reading that feature's own `property_api_settings` columns (`<name>_sync_enabled` / `_hour` / `_minute` / `_last_date`).

Frontends follow it too: a **Live API / Database** mode toggle (same as Bills), an "Import To Data Mart" button, and a date/property control block.

**Whole-blob encryption.** `st_files_sync`, `rv_files_sync`, `rr4_tm30_sync` and `bcp_snapshots` all store `data = {"blob": encrypt(json.dumps(report))}` rather than encrypting named columns, because the PII (guest names, passport/ID numbers, birth dates, billing descriptions quoting guest names) sits nested inside lists that `encrypt_data`'s flat key scan can't reach. `occupancy_sync` is the exception and stores plain `jsonb` — its payload is category names and integer counts with no PII in it. `bcp_snapshots` additionally **gzips before encrypting** (a busy property's ±7-day Timeline measured ~3.4 MB decoded); the row carries a `"gzip": true` flag so older uncompressed rows still decode.

### Scheduled jobs

**Five separate Vercel Cron entries** (`vercel.json`) — three every 5 minutes, two every minute:

| Cron path | Schedule | Drives |
|-----------|----------|--------|
| `/api/sync/auto` | `*/5` | Everything schedule-driven except the rows below — see the list under it |
| `/api/bcp/auto-capture` | `*/5` | BCP snapshots only, kept separate so BCP's 5-minute cadence doesn't drag a full data sync along |
| `/api/sync/retry-check` | `*/5` | `retry_scheduled_syncs` only, split out so the Retry Policy interval can be configured in **minutes** and actually resolve at that granularity |
| `/api/sync/st-files-auto` | every minute | `daily_auto_sync_st_files` only, so each property's configured ST minute is the minute it pulls rather than the next 5-minute mark |
| `/api/sync/kiosk-arrivals-auto` | every minute | `auto_sync_kiosk_arrivals` — the kiosk check-in list mirror (see `/kiosk`). Runs on **every** tick with no `_last_date` gate, and deliberately **not** under `acquire_sync_lock`: that lock is shared with `daily_auto_sync`, which skips a property whose lock is held, so taking it every minute would eventually make the day's real Data Mart import skip |

Several docstrings in `main.py` still describe `/sync/auto` as the *hourly* cron — that is stale; `vercel.json` is the authority. Firing every 5 minutes is safe because each job self-gates on its own `*_last_date` marker column and only does work once per day.

`/api/sync/auto` fans out (as FastAPI `BackgroundTasks`, so it returns immediately) to: `daily_auto_sync` (the 5-table Data Mart sync), `retry_failed_syncs`, `daily_auto_sync_st_files`, `daily_auto_sync_occupancy` and `daily_auto_sync_occupancy_pm` (occupancy's morning and afternoon captures), `daily_auto_sync_rr4_tm30`, `daily_auto_sync_rv`, the four email jobs (`send_st_files_daily_email`, `send_st_files_per_property_emails`, `send_rr4_tm30_daily_email`, `send_rr4_tm30_per_property_emails`), `send_ftp_upload_job`, the two verification mails (`send_st_compare_email`, `send_rr4_compare_email`) and `send_stop_sale_alert_email`. Each self-gates against its own per-property schedule columns.

**Locally** an APScheduler starts on FastAPI startup and ticks each of these every minute (`second=0`), plus BCP every 5 minutes. `start_scheduler` **returns early when `os.environ["VERCEL"]` is set** — without that guard, every Vercel cold start (any request, not just the cron) spun up a fresh scheduler that ticked for whatever arbitrary minute the cold start landed on, which is why auto-sync used to be unreliable in production.

**`match_hour_only`.** In production, jobs are invoked with `match_hour_only=True` because a cron firing is not guaranteed to land on the exact configured minute. Local ticks match to the minute. `retry_scheduled_syncs` splits the difference: it buckets to 5-minute marks under Vercel and to the minute locally.

**Two different retry mechanisms, don't confuse them:**
- `retry_failed_syncs()` — fixed, once daily at 09:00 Asia/Bangkok (self-gated on `hour == 9`), re-runs any (property, table) pair whose latest `sync_logs` row *today* is still `error`. Logged as `sync_type="retry"`.
- `retry_scheduled_syncs()` — per-property, tied to each property's *own* schedule plus configurable offsets (`sync_retry_settings`, Admin > Sync's Retry Policy card; default 2 retries 60 minutes apart). Covers all four independent schedules (Data Mart, ST Files, RR4/TM30, RV). Retries a table that is `error` **or has no `sync_logs` row at all** for today — the scheduled run never firing (cold start, outage) looks identical to a failure from the outside and matters just as much. No dedup logic is needed because every sync upserts on `mews_id`.

Concurrency is guarded by a DB-backed lock (`sync_locks` table + `acquire_sync_lock` / `release_sync_lock` RPCs); BCP captures take the same lock with a 4-minute timeout. `daily_auto_sync`'s five per-table syncs (Reservations/Customers/Payments/Resources/Bills) are extracted into module-level `_sync_*` functions precisely so the retry paths can re-run one table without re-running the property.

### Frontend routing (`src/app/` — Next.js App Router)

- `/` — Login page (Supabase Auth)
- `/dashboard` — KPI overview, plus a per-property import-status card whose "Import Latest" button posts `POST /sync/property` (re-runs exactly what that property's scheduled `daily_auto_sync` would have, tagged `sync_type="manual"`, and synchronously so the button can report a result)
- `/data-mart` — Synced data from `reservations_sync`/`members_sync`/`payments`, rendered by the shared `src/components/DashboardView.tsx`, which enforces a per-section column order via `SECTION_COLUMNS` (58 columns for reservations, matching the MEWS Reservation Report schema). A MEWS/Data Mart toggle switches to live MEWS fetches (`/api/*/live`) with an "Import To Data Mart" button; the old standalone `/live-data` page was removed in favor of this toggle
- `/managed-members`, `/managed-payments` — Synced members/payments views
- `/log-import` — Import history
- `/profile` — Own-account page; avatar upload goes to the Supabase **Storage** bucket `avatars` and is read back with `getPublicUrl`
- `/bill-generator` (sidebar label "Bills") — Lists MEWS bills per property/date range, toggled between "Live API" (`bills/getAll`, always current) and "Database" (`bills/managed`, reads `bills_sync` — faster once a range has been backfilled via "Import To Data Mart"); "NHG Bill" opens `/print-bill/{id}` (or `/print-bill/batch?ids=a,b,c` for multiple), "MEWS Bill" fetches MEWS's own generated PDF (`bills/getPdf`)
- `/print-bill/[id]` — Fetches full itemized invoice data (`GET /bills/{id}/invoice`) and the property's HTML template (`GET /bills/template`), then does `<<Token>>` string substitution and renders on-screen via `dangerouslySetInnerHTML`; printing is plain `window.print()`. Token substitution (`renderInvoiceTemplate`), the `Invoice` type, and the `@page`-A4/page-break print CSS (`INVOICE_PRINT_CSS`) live in `src/lib/invoiceTemplate.ts`. `get_bill_invoice` (`sync_service.py`) checks `bills_sync` first and only falls back to live `bills/getAll`+`orderItems/getAll` if the bill isn't cached; `payments/getAll` is always called live since `payments` has no queryable Bill Id column. **Print pages and the app shell**: print-bill/print-rr3 render inside `Navigation.tsx`'s shell, whose inner `overflow-y-auto` wrapper would rasterize its scrollbar onto every printed page — `globals.css`'s `@media print` block (aside hidden, `main, main > div` overflow reset, webkit scrollbars hidden, white body) exists to prevent exactly that; don't remove it. A server-side PDF route (headless Chromium via `playwright-core`+`@sparticuz/chromium`) was built, production-verified, then **removed by user decision** (server cost — Chromium needed a 3GB function) once browser printing was fixed; if it's ever wanted again, recover from git (`9f9de79`→`e7fd06b`) and note WeasyPrint (needs native Pango/GTK, unavailable on Vercel Python) and `xhtml2pdf` (cannot render Thai at all) were both tested and ruled out
- `/rr3` — Lists guests checking in for a property/date range (via `GET /rr3/cards`, which joins Reservations+Customers+Resources in one live MEWS call — see `sync_service.get_rr3_cards`); "Print All" opens `/print-rr3` to print every Thai Hotel Act ร.ร.๓ lodger registration card in one document. The card layout is a single shared HTML template for all properties (`GET/POST /rr3/template`, `rr3_templates` table keyed by a fixed sentinel row, edited at Admin > Email Template, RR3 tab — no per-property picker, unlike Billing) with `<<Token>>` substitution done client-side in `/print-rr3`; the default (`DEFAULT_RR3_TEMPLATE` in `api/app/routers/rr3.py`) matches the official government blank form, and the template GET falls back to it if the table is missing so printing never breaks
- `/rr4-tm30` (sidebar "RR4/TM30") — Two statutory registers built from **one shared MEWS fetch** (`sync_service._rr4_tm30_fetch_day`) and stored in **one shared row** (`rr4_tm30_sync`, `{"rr4": ..., "tm30": ...}` under a single encrypted blob) — halving the MEWS cost, since both always describe the same day. `/rr4-tm30/preview` renders the export before download; `GET /rr4/export` and `GET /tm30/export` produce `.xlsx`.
  - **RR4 (ร.ร.๔ guest register)** — every guest whose stay *overlaps* the day, Thai and foreign alike. Overlap is the standard interval test (`StartUtc < window end AND EndUtc > window start`), deliberately **not** ST Files' Customers-tab rule, which only works by coincidence when the window ends at midnight and silently dropped 61 guests once Chinatown's real 12:15 window was configured. Check-in times use `ActualStartUtc`, never the deprecated scheduled `StartUtc`. **Rows with nothing in columns E–K (every name column) are dropped from the `.xlsx` and the remaining rows renumbered** (`sync_service._rr4_filed_rows`) — they are MEWS's own unnamed occupant slots, one per booked headcount whose companion profile was never attached, and a row with no name is not a lodger the Act asks us to register. The generator sheet keeps them (blanked by its `if(<nameEn>="","",…)` wrapper), so the verification mail still counts them on both sides. The drop happens at **render** time, not in `get_rr4_report`, so the Edit page still lists a nameless slot and someone can type the guest in — once named it joins the filing on its own. Anything reporting a row count next to the file (the daily email's table) must count `_rr4_filed_rows`, not the report.
  - **TM30 (foreign-arrival notification)** — guests *arriving* that day, non-Thai only, filed by their **booked `ScheduledStartUtc`** (fetched from `reservations/getAll/2023-06-06`), **not** the un-versioned `StartUtc` the day's fetch returns — MEWS moves `StartUtc` to the real check-in when a guest arrives early, and the sheets file by the booking. The window is `(start, start+24h]` where start is `tm30_day_start_hour/_minute` per property, and those values should simply **mirror each sheet's own declared start (Master!B2)**. That is not a guess: on 17-Sep-2026 the scheduled clock plus each sheet's declared start matched all five comparable properties by passport (Patong 81/81, Samui 126/126), where the moved `StartUtc` could not match Patong or Samui at *any* start time. Every earlier "window" flip-flop (Patong 02:05 → 00:00 → 02:05) was this wrong clock, not a wrong time — Patong's W106185 read `StartUtc` 10-Sep 00:57 but was booked for 14:00. Re-measure with `scripts/tm30_window_sweep.py` (scores window shapes × clocks per property by passport) and `scripts/tm30_missing_named.py` (names the guests behind a gap) before moving any start time again.
  - **The day window** (`property_api_settings.rr4_tm30_day_start_hour/_minute`, `rr4_tm30_day_end_hour/_minute`, all defaulting to 0) exists to mirror MEWS's own native "Customer profiles" report window per property, which is why it has minute precision. It is *not* constrained to 24h — a safety constraint forcing that was explicitly reverted, so **a stale non-zero value here silently undercounts the register** (a leftover 14:00/12:00 on Chinatown once produced 160 rows instead of 241). Double-check any non-zero value.
  - **Manual corrections** (`RR4 & TM30 Files > RR4|TM30 > Edit` → `/rr4-tm30/edit`, styled after Admin > RR4-Nationality) live in their own `rr4_tm30_overrides` table, **not** in the `rr4_tm30_sync` blob — "Re-Generate Files" and the nightly import both overwrite that blob wholesale, so an edit stored there is silently un-done on a document that may already have been filed. `sync_rr4_tm30_day` is the one caller that passes `apply_overrides=False`, keeping MEWS's raw answer intact so a reset needs no re-fetch; every read path (`get_rr4_report`/`get_tm30_report` default, `read_managed_day`, `_read_rr4_tm30_cached_day` for the email) lays the corrections back over it. Rows are addressed by `_key` = `<ReservationId>:<CustomerId>` (or `:placeholder:<n>`), never by the positional line number — a day imported before `_key` existed has to be re-generated once before it can be edited, which the editor detects and offers up front. Editable columns are served from `GET /rr4/edit-columns` off the export's own `_RR4_COLUMNS`/`_TM30_COLUMNS`, so the editor cannot drift from the filed form; overrides may introduce a column MEWS never sets (the four Thai-name columns are always blank), which is why applying assigns rather than only replacing.
  - Nationality codes: neither form uses ISO alpha-2, which is all the Connector API gives. `rr4_nationality_codes` / `tm30_nationality_codes` (editable at Admin > RR4-Nationality and TM30-Nationality) layer over the hardcoded fallbacks in `api/app/services/rr4_tm30_reference.py`, which were reconciled against the real generator sheet and carry inline evidence for each hand-corrected entry. An unmapped code renders blank rather than blocking the row.
- `/st-files` (sidebar "Statistic Files") — Daily occupancy report per property + single Bangkok date, replicating the old manual "Chinatown-ST" Google Sheet: 8 underline-tabs (Spaces / Occupied / House Uses / Out of Order / Availability / Customers / Arrivals / Departures). Backend is `GET /st-files/report` → `sync_service.get_st_files_report`, which joins 6 MEWS calls: `services/getAll` (resolve the Bookable ServiceId), `resourceCategories/getAll` (**requires `ServiceIds` in the payload and the Resource Categories permission on the property's Connector token** — 401s if MEWS hasn't enabled it), `services/getAvailability/2024-01-22` (Occupied/HouseUse/OutOfOrderBlocks/ActiveResources per category), legacy un-versioned `services/getAvailability` (MEWS's own precomputed free-to-sell number — deliberately not derived by hand), `resourceBlocks/getAll` (named OOO/house-use rows), and the RR3-style `reservations/getAll` Extent join. Category tabs only count `Type in (Room, Bed)` — verified to reproduce the sheet's totals exactly (Chinatown = 176). `GET /st-files/export` emits the legacy pipe-delimited `PMSST|RMSST` submission file (10 fixed metric rows, 41 fields each), named `{property_code}_ST_{yyyymmdd}.csv` from `property_api_settings.st_property_code` — which **raises rather than guessing** if that property has no code yet, since this is a real submission. The **Complimentary** metric (`sync_service._is_complimentary`) is `State == "Started" AND (Rate in {"Complimentary", "Complimentary Room"} OR BusinessSegment == "Complimentary")` — both arms matter and the Segment arm is the reliable one: on 25-Aug-2026 it was right on all four real complimentary rooms while Rate caught only two, because a comp room can sit on an ordinary commercial rate with nothing but the segment marking it (Patong #156361, rate "Group Leisure Series - Room Only"). Segment names are `.strip()`ed — MEWS stores them as typed and several carry a trailing space. Siem Reap's own sheet uses a narrower Rate-only formula that is deliberately **not** reproduced. The **Arrivals** metric has no MEWS figure to read at all and is derived from each reservation's own scheduled `StartUtc`, with one exception: a **zero-night ("day use") stay counts as an arrival unless its ROOM is handed to another stay later the same day** (`sync_service._ST_DAY_USE_ROOM_TURNOVER`, applied by `space_turns_over_today`) — if the room turns over, that next stay is the room's arrival for the day and this one is the tail of the previous night's business wearing today's date, still a **departure** (it checks out during the day) but never an arrival. **Established 18-Sep-2026**, replacing three successive clock-based rules that were each shipped and then contradicted within days (a scheduled-start cutoff, an actual-check-in cutoff, and a checkout-time cutoff were all tried). It is the first rule that fits **every** reservation on record — seventeen named day-use stays across 10, 16 and 17-Sep-2026 and five properties, listed in the comment block above the constant — and it **resolves** what that comment previously called an unresolved conflict: Chinatown #97796 (counted) vs #97770 (excluded), 10-Sep-2026, share a scheduled `StartUtc` of 00:00 so no clock could separate them, but 405 was re-let at 14:00 that afternoon and 301 was not. Why the clock rules kept looking right: a room that is turning over usually holds a stay booked to the standard noon checkout, so "ends at 12:00" and "starts before 02:00" correlate with turnover without causing it — 17-Sep Siem Reap #152050/#152051 (out 09:15/09:27, both re-let at 14:00, both excluded by the sheet) are the pair that ruled out every end-time cutoff. Verified 18-Sep-2026 against all 8 sheets per space category: **7/8 properties match on every Arrivals and Departures category**, the one residual being Makati `DKR -1, DTR +1` — a single reservation (#228962) booked as King and put in a Twin room, which MEWS's Availability report files under the *requested* category while we (and MEWS's own Reservation export) file under the *assigned* room's; it nets to zero on the total, so the submitted file and the daily mail's Arrivals total are unaffected. The **Customers** metric counts people, filed under the **assigned space's own category** (`sync_service.customers_category`) — *not* the requested one, and *not* the child fallback `space_categories()` uses for Occupied. Occupied spreads a whole-unit booking (a connecting suite, a whole dorm) across its child rooms; Customers does not follow it there, so such a booking adds **0**. Siem Reap 10-Sep-2026 is the proof: its sheet files DCR with 12 Spaces, 12 Occupied and **0 Customers**. Verify any of this against the sheets with `scripts/st_compare.py`
- `/rv` (sidebar "Revenue Files") — The revenue/payment counterpart to ST Files: `sync_service.get_rv_report` builds a daily journal from `orderItems/getAll` (by `ConsumedUtc`), `payments/getAll` (by `CreatedUtc`) and `outletItems/getAll`. `GET /rv/export` emits the pipe-delimited Infor `PMSRV` journal in the same 41-field layout as the ST file. Three things here were each a real bug found against a real file, not theory:
  - **Order items with `AccountingState == "Canceled"` must be dropped.** MEWS keeps the superseded posting next to its replacement; on one Chinatown day that was 95 of 224 accommodation items and ~197k of phantom revenue (2.4× overstated). Payments have no such twins.
  - **Outlet (POS) tills are a separate MEWS ledger** and never appear in `orderItems`/`payments` — `outletItems/getAll` is a whole additional revenue source, not a detail.
  - **VAT is summed from each item's `Amount.TaxValues`**, never derived from a rate, because MEWS already splits mixed-rate items. Some properties stack a duplicate service-charge component into the same array (Makati), so the chart's `vat_tax_codes` restricts which codes count as output tax, and `secondary_tax` captures Thailand's separate 1% provincial tax.
  - **GL accounts are per-property and not interchangeable** (Siem Reap books Guest Ledger to 11401 where Chinatown uses 21203). Resolution order is `rv_gl_mappings` (pulled from MEWS's own `accountingCategories/getAll` via `POST /rv/gl-mappings/sync` — `LedgerAccountCode`, not `Code`, is the real account) → the hand-verified `_RV_CHARTS` dict in `sync_service.py` → fallback. Export refuses outright for a property with neither, rather than borrowing another property's codes; on-screen viewing still works
- `/revenue` — Occupancy % per space category per night (`sync_service.get_occupancy_report`, from `services/getAvailability/2024-01-22`, which returns a value per time unit so a range costs one call per ~99-day slice — MEWS rejects a 100-day interval outright), plus a **Rate** tab priced from MEWS's own default BAR rate and an **Occupancy By Type Calendar** with an adjustable stop-sale threshold, plus (added 23-Sep-2026) per-date-range **peak-period overrides** — e.g. December stops selling at 90% except 23–31 Dec, which stops at 50%. Persisted in `stop_sale_periods` (one row per property per period; a property can have several, including several inside the same month). **The on-screen field is one value PER MONTH** (`stopThresholdByMonth`, keyed by month, 24-Sep-2026 — it used to be a single value shared by every month, changed at the user's request so adjusting September no longer moves October along with it), falling back to `DEFAULT_STOP_SELL_THRESHOLD` for a month never saved; the PIN-unlock flag itself stays a single shared flag, since entering the PIN unlocks editing in general rather than one specific month's box. **It now auto-saves, per property, per month** (`stop_sale_month_thresholds`, same day) — the field was deliberately session-only before that (the comment above used to explain why: it changes the business definition of stop-sale and shouldn't quietly become a permanent setting nobody remembers changing), but the user asked for it to persist anyway, since in practice a month's value is set once and kept resetting on every reload was itself the more surprising behaviour. `stop_sale_month_thresholds_service.save_threshold` is a read-modify-write merge (one `thresholds` jsonb column keyed by `"YYYY-MM"` per property), so saving one month can never clobber another's already-saved value, and the frontend debounces each month's own save (500ms) so dragging the number input's spinner doesn't fire a request per tick. Reading needs no PIN (same as Room Types/peak periods); every write is checked server-side against the same Stop-Sale PIN. Needs `api/sql/stop_sale_month_thresholds.sql`; until it's run every month's field resets to the default on reload, exactly as before this existed. `thresholdForDate()` resolves the threshold for one night as a matching saved period, else that night's own month's on-screen value; periods aren't scoped to one calendar month (a period can span a month boundary) and aren't expected to overlap, so the earliest-`start_date` match wins if they ever do. `thresholdForDate()` is the single choke point both the on-screen grid and the export/print builder read through — an earlier version of the on-screen grid called `stopState()` directly with the raw threshold instead of through this function, so a peak period colored the exported file but not the calendar itself; fixed same day. Reading the list needs no PIN (matching the on-screen field, visible to anyone who can see the calendar); every add/edit/delete is checked **server-side** against the real Stop-Sale PIN (`occupancy.py`'s `_require_stop_sale_pin`), a deliberately *stronger* check than the on-screen field gets — that value is never persisted at all, so there's nothing for a bypassed gate to reach, where these rows are real, durable state a client hitting the API path directly could otherwise write regardless of the browser's own PIN-unlock flag. **Neither the peak periods nor the per-month on-screen threshold are wired into `stop_sale_alert_service.py`** — the daily "new stop / re-open" mail compares every property against the flat `DEFAULT_THRESHOLD = 90` constant regardless of either, a known and deliberate scope boundary for this pass, not an oversight; extending the alert to honour these too is separate work. Needs `api/sql/stop_sale_periods.sql`; until it's run the feature degrades quietly (GET returns an empty list, every night falls back to the single threshold, exactly as before).

**Room Types is one shared, persisted selection, not per-month, and it DOES reach the alert email** (24-Sep-2026). The chart used to filter each month's own table independently (client state, reset every session); at the user's request it is now one Set shared by every month — pick it once and every month's table (and the export/print builder, since both read the same `categoryRowsForMonth`) reflects it — and it is saved per property in `stop_sale_watched_categories` (`property_name` primary key, `categories` a jsonb array of category ids, or no row/`null` = watch everything, the default). Reading needs no PIN (matching the single threshold field); every **write** is checked server-side against the same Stop-Sale PIN `stop_sale_periods` uses (`occupancy.py`'s `_require_stop_sale_pin`, reused rather than a second PIN) — this now decides what the daily mail reports on, not just what one browser session's chart shows, so it's real, durable state and not a view-only convenience anymore. `stop_sale_alert_service.py`'s `_changes()` takes the saved list as `watched` and skips any category not in it (`watched=None` → everything, unchanged from before; `watched=[]` is a real, distinct "nothing", not "everything"). Needs `api/sql/stop_sale_watched_categories.sql`; until it's run, GET returns `null` and both the chart and the mail watch every category, exactly as before this existed. The Total row sums occupied and active across categories rather than averaging percentages — a 40-bed dorm at 20% must not weigh the same as a 4-room category at 100%. Categories are filtered to the property's own ST space types for the same reason ST Files does it (a parent Dorm category covers the same physical beds as its child Bed category). Snapshots go to `occupancy_sync` as plain `jsonb`, anchored to **whole months** (`occupancy.snapshot_range`: first of the capture's month through 12 months forward) because the calendar reads a month at a time and a snapshot starting on the 21st left days 1–20 blank. Captured **twice a day** — 08:00 and 13:00 Asia/Bangkok by default (`main.daily_auto_sync_occupancy` / `daily_auto_sync_occupancy_pm`, each its own hour/minute/last-date columns on `property_api_settings` so neither run blocks the other) — since two captures a day let a revenue manager see what moved since the morning, not just since yesterday. `report_date` alone no longer identifies a row (two captures the same day share it), so writes are a plain **insert**, never an upsert — every capture, scheduled or a manual re-import of the same date, is its own row — and every reader that used to assume one row per date (`GET /occupancy/managed`, the frontend's Snapshot Date picker) now addresses a specific snapshot by its own `id`. Pruned to the newest 6 per property (3 days of history at 2/day) — and pruning deliberately does *not* happen inside `sync_occupancy_day`, so a manual import of an older date for comparison survives until the next scheduled run
- `/reconciliation` — The one module with no MEWS or Supabase involvement at all: two file-upload tools backed by `api/app/services/reconciliation_service.py` (pandas + openpyxl). `POST /reconciliation/gl-split` splits a GL Account Detail export into a per-account-code workbook (All Transactions / Outstanding / Offset, where offsets are greedily paired transactions summing to zero) zipped together; `POST /reconciliation/bank-gl-match` matches Bank Statement rows to GL rows by amount with a ±1 day tolerance. Header rows are *found* by scanning for the required column names, not assumed at a fixed index, because the exports carry title/metadata rows above them
- `/bcp` — Mews Business Continuity Plan: read-only snapshots (captured every 5 minutes) of a wide reservation window so front desk can keep working from the latest copy when MEWS is down. Backend `sync_service.get_bcp_snapshot` builds a MEWS-style Timeline: one `reservations/getAll` Extent call spanning today ±7 days (`_BCP_WINDOW_DAYS_BACK`/`_BCP_WINDOW_DAYS_FORWARD`) feeds room-row × date-column bars, joined against `orderItems/getAll` (rate-vs-product charge breakdown, gross/net, chunked 100 IDs at a time) and `serviceOrderNotes/getAll` (permission-gated — degrades to empty notes if the token lacks it), plus window-wide deduplicated lookups for reservation groups/rates/companies/business segments. Room rows come from `resources/getAll`, grouped/ordered by MEWS's own `resourceCategories.Ordering` field (not alphabetically — getting this wrong previously mis-ordered a live property's category groups), retried once on failure since a second failure silently collapses the grouping to empty; a resource with a `ParentResourceId` (e.g. a dorm room split into individually bookable beds) nests directly under its parent regardless of its own category. `Reservation.Origin`'s combined enum string (e.g. `CommanderInPerson`) is parsed back into MEWS's own "Reservation source" label, with a separate branch for channel-manager/OTA bookings whose Origin format differs entirely. Frontend `/bcp/page.tsx` has **Timeline** (the grid, with date navigation and space search; clicking a reservation bar opens a detail panel with group/category/rate/company/travel-agency info, the expandable rate/items charge breakdown, notes, and a room-lock padlock indicator; clicking a room opens its own state/category panel) and **Payments** (today's `payments/getAll`) tabs, plus a print-only plain table (`hidden print:block`, triggered by "Print Housekeeping Sheet", with a "Cleaned ✓" tick column) since the on-screen grid doesn't paginate for print.
  - **Two storage layers, opposite retention policies, and this distinction matters.** `bcp_snapshots` is the *disposable* read-only copy: gzipped + Fernet-encrypted, pruned to the newest `SNAPSHOTS_KEPT` (12) per property, i.e. 1 hour at 5-minute intervals — deliberately short, since at the original 48h/576 it was by far the largest table in the database and drove a Supabase disk auto-expand. Everything front desk *enters while MEWS is down* goes to its own **durable, never-pruned** tables instead: `bcp_action_logs` (the Check In/Out, Chg Room, Room Status, Reg Card Saved audit trail, with frozen copies of the reservation and guest profile as they stood at the time), `bcp_reg_cards`, `bcp_reservation_notes`, `bcp_room_changes`, and the `bcp_*_overrides` family (room status/number/type, arrival, billing, guest, room lock). Any new BCP feature that captures user input follows this: durable table + Action Log visibility, never the snapshot blob. `action-logs` is queried per *property*, not per day — an unresolved action shouldn't stop being flagged because the date rolled over.
  - Router `bcp.py`: `GET /bcp/live` (build fresh, don't store — the UI's fallback when history is empty; useless once MEWS is actually down), `POST /bcp/capture`, `GET /bcp/snapshots` + `GET /bcp/snapshot?id=` (history picker), `GET /bcp/last-capture`, plus GET/POST pairs for each override table and `action-logs` (+ `/toggle`, `/archive`, `/delete`). `GET /bcp/auto-capture` is its own dedicated Vercel Cron entry; each property takes the same `acquire_sync_lock`/`release_sync_lock` RPCs `daily_auto_sync` uses (4-minute timeout) so an overrunning capture can't overlap the next tick, and captures log to `sync_logs` **only on failure** (a success row per property per 5-minute cycle would drown the Activity Log)
- `/users-report` — Read-only twin of Admin > User Management's Users tab (same columns, search, sort and .xlsx export; no create/approve/edit/delete and none of their modals), as its **own main-sidebar menu** gated by `role_permissions.users_report`. It exists so a role can be shown the user directory without being given the Admin menu, which would carry every other admin page with it. Unlike the other main menus it is **route-guarded** in `Navigation.tsx`, not merely link-hidden — same reasoning `/admin/*` has, for the same kind of content — and it is the one field the hardcoded `role_permissions` fallback leaves `false`: that fallback exists so a missing row can't strand someone with an empty sidebar, which is not a reason to hand out every account's email and sign-in history. Super Admin is **not** special-cased (ordinary sidebar menus read straight from the table), so `api/sql/users_report_menu.sql` switches the column on for it explicitly
- `/kiosk` — Self-service guest check-in terminal, ported from the standalone NHGKiosk prototype (`github.com/kitti2424/Kiosk`) and currently **screens only**: welcome → search → registration → eKYC → upsell → payment → success. Search, Confirm and Registration read **real reservations** (below); eKYC onward still run on mock data, and nothing writes back to MEWS yet — no screen performs an actual check-in. Three things were changed on the way in and should stay that way: the prototype's own `properties` table is gone (property name comes from `useSelectedProperty()`, so the kiosk follows the app's property switcher, and shows in the header exactly as it does on every screen's own content), its dark palette moved off `body` into a scoped `.kiosk-root` class in `globals.css` (declaring it on `body` would have repainted every other page in the app), and its `/` links now point at `/kiosk` — `/` is this app's login page. `Navigation.tsx` renders these routes with **no sidebar or top bar**, the same standalone treatment `/reset-password` gets, but *inside* the auth guard and behind a `role_permissions.kiosk` route guard (not merely a hidden link — a full-screen screen that looks like the property's own front desk shouldn't be reachable by typing the URL). `api/sql/kiosk_menu.sql` adds the column and switches it on for Super Admin. The prototype's own admin pages, API routes, login page and per-slug duplicates were deliberately **not** copied. Its device-pairing screen (`/kiosk/lock`, a PIN/QR gate over a `localStorage` flag) was ported and then **removed** once it shipped: it gated only the header's property label, not any route or real content, so the header read "Device Secured" while every screen's own content already showed the selected property — a demo lock in front of a door with no lock on it. Real per-terminal pairing, if wanted later, belongs keyed to `kiosk_settings` (Admin Console > Kiosks) rather than a browser flag. Adds one dependency used nowhere else: `framer-motion`. **Welcome → Search → Confirm → Registration are their own animal**, redesigned 17-Sep-2026 against real reference screenshots of the terminal in the field: a light theme, not the dark one eKYC-through-success still use, sharing `KioskTopBar.tsx` (Staff Mode badge, language/currency pills, a full-screen toggle, an optional back button via `showBack`) instead of the old flow header — `KioskLayout`'s `LIGHT_THEME_ROUTES` list (`/kiosk`, `/kiosk/search`, `/kiosk/confirm`, `/kiosk/registration`) makes each of those bypass the dark shell's header/footer/progress-bar chrome entirely and render itself full-screen; eKYC/upsell/payment/success have no reference screenshot yet and deliberately keep the old dark shell so nothing regresses. Welcome's Check In button goes to `/kiosk/search` — a **reversal** of an earlier same-day decision to skip search straight to registration, made because search itself was redesigned that same day from a find-my-booking form into a browse-today's-arrivals list (below), which is worth keeping in the flow rather than skipping past. `/kiosk/search` lists today's arrivals as cards (name, headcount, arrival/departure) filtered by a name search box; picking one carries only the MEWS reservation id forward as `?guest=<id>`. `/kiosk/confirm` (new) reads that reservation, greets the guest by name, and shows a one-screen booking summary (requested space category, scheduled checkout in the property's own timezone, and an **Included** list) before continuing to `/kiosk/registration?guest=<id>`. **Included**, added 17-Sep-2026 against a real reference screenshot: `sync_service.get_kiosk_included_items` makes one *live* `orderItems/getAll` call per reservation (on demand from `GET /kiosks/arrivals/{id}`, not the per-minute mirror — a single lookup, not the whole lobby polling) and groups `Type == "ProductOrder"` rows by `BillingName`. MEWS exposes no "this product is included in the rate" flag, so the split is inferred from shape: a billing name repeated across **more than one** order-item row is one the rate auto-generates per night (`"5x Service Charge Nightly"`, `"5x Breakfast Service Charge"`, `"5x Breakfast Include in room (Adults)"` on a real 5-night Marasca Samui booking — matched exactly against a live reservation, all three counts equal to nights stayed); a name appearing **exactly once** is a standalone purchase (`Mixology Masterclass`, `Bottle of sparkling wine` — both timestamped to the guest's actual check-in moment, not spread across the stay) and is excluded. A genuinely 1-night stay's real included breakfast can't be told apart from a 1-off purchase under this rule, since MEWS gives no better signal — a known, documented limitation, not a silent bug. **Where the list comes from (17-Sep-2026, Marasca Samui only):** a per-minute mirror, not a live MEWS call per screen. `sync_service.get_kiosk_arrivals` fetches every reservation on the property's accommodation service (`_resolve_stay_service` — Siam's co-working space is bookable too, and nobody checks into it) whose **booked `ScheduledStartUtc`** falls on the property's local today, plus its customer and requested category; `kiosks.sync_kiosk_arrivals` upserts that into `kiosk_reservations_sync` and deletes today's rows **stamped before the run began** (i.e. those MEWS no longer returns) plus every earlier day. Deleting by stamp rather than by "id not in this run" is what keeps two overlapping runs safe, and a MEWS or write failure raises before any delete, so a bad minute leaves the last good list on screen instead of an empty kiosk. `GET /kiosks/arrivals` then serves **`State == "Confirmed"` only, newest reservation number first** — unless `include_all=true`, which the search screen's **"Show all reservations" switch** (beside the count, off by default, added 18-Sep-2026) sends to get every one of today's arrivals in any state; those non-Confirmed cards carry a translated status badge (`KioskCopy.stateLabel`) and are disabled, since only a Confirmed booking can be checked in. The switch is the search page's own local state, not a context, so leaving the screen always returns a lobby terminal to the check-in-only default — and `GET /kiosks/arrivals/{id}` serves one reservation (with email, which the list deliberately omits since it's on screen for anyone at the terminal). That rule was not guessed: MEWS's own kiosk screenshot for Marasca Samui, taken 14:46 Bangkok on 17-Sep-2026 (the iPad's clock reads 00:46 because it's set to US Pacific), listed exactly the six reservations that were Confirmed at that minute, in descending number order, and all fifteen others were already checked in (Nizan 14:42, Riccardo 14:31, Noam 11:12, Claire 07:28), created later (#50885, 16:47) or canceled. The search screen re-polls every 30 s (`ARRIVALS_POLL_MS`) and keeps the list already on screen if one refresh fails; confirm/registration load their reservation once and say so plainly if it's no longer Confirmed (checked in at the desk meanwhile). Dates use `Intl` in the guest's chosen language and the row's stored `time_zone`, which reproduces MEWS's English exactly ("Thu, Sep 17", "Friday, September 18, 12:00 PM"). Enabling another property is one `update property_api_settings set kiosk_arrivals_sync_enabled = true`; before `api/sql/kiosk_reservations_sync.sql` has run, the job skips quietly and the search screen says self check-in isn't available. `/kiosk/registration` was fully rebuilt from the old dark "Review & Signing"/PDPA design into a two-panel layout: a guest+progress card (with a static, non-functional "Tap to return skipped guest" affordance — this flow only ever holds one guest, so there's nothing to actually skip between yet) and an "Enter your details" form (email, a gating Terms checkbox, a marketing opt-in checkbox, and a tap-to-sign signature placeholder with no real capture behind it). Welcome's bottom-left caption shows this kiosk's own **configured name** (Admin Console > Kiosks) rather than a reference screenshot's real MEWS build number (`v4.53.0 (25001955)`), which would misrepresent this prototype as running MEWS's own software if copied verbatim — this is also the concrete example of "config must always relate to the front end." Font is explicitly `font-sans` (IBM Plex Sans); an exact match to a native app's system font can't be verified from a screenshot alone. **Theme and language were wired up for real 17-Sep-2026** after the user pointed out that changing Theme in Admin Console > Kiosks did nothing on screen: the four light-theme screens no longer hardcode `#F4F4F5`/`#0B0B0F`/`#4F46E5`; they read `--kiosk-bg`/`--kiosk-surface`/`--kiosk-text`/`--kiosk-accent`/etc. from `globals.css`'s `.kiosk-scheme` block, which `kiosk/layout.tsx` switches between its light and dark variable sets via `data-kiosk-theme={config?.theme === "Dark" ? "dark" : "light"}` — so the admin's Theme field now actually does something, scoped to these four screens only. **Language is a real, working switcher, not a static label**: `KioskTopBar`'s language pill opens a dropdown over `src/app/kiosk/i18n.ts`'s five languages (English, Thai, Filipino, Khmer, **Japanese**, added same-day at the user's follow-up request), each with translated copy for all visible text on the four screens; `KioskLanguageProvider` (`kioskLanguage.tsx`, mounted in `layout.tsx` above the routed pages) holds the current choice in memory for the session, seeded from the kiosk's configured `default_language` (`resolveDefaultLanguage`) but overridable by the guest at any time — it is deliberately *not* saved back to `kiosk_settings`, the same way a real terminal's language picker doesn't change what the next guest sees. Admin Console > Kiosks' `LANGUAGES` list was extended to the same five so its Default language dropdown can actually set one of them. Three `KioskCopy` fields (`welcomeTitle`, `helloGreeting`, `noGuestsMatch`) are **functions, not plain strings**, specifically because Japanese word order puts the name before the phrase ("Xへようこそ", not "Welcome to X") — a fixed prefix-then-name concatenation that works for the other four languages can't represent that, so those three return the whole sentence for the caller to render as one unit rather than a fragment to glue a name onto. **The Thai/Filipino/Khmer/Japanese copy in `i18n.ts` is a first-draft prototype translation, not reviewed by a native speaker of each language** — Thai is the most confident, Khmer and Japanese the least; get it checked before any real guest relies on it. **The "Switch to guest mode" toggle was removed** (`KioskTopBar`) at the user's request since it had no real behavior behind it yet — "Staff Mode" is now a plain non-interactive badge until there's an actual guest-mode view to switch to. **The currency pill reads MEWS**, unlike the language pill: `GET /api/kiosks/currencies` → `sync_service.get_kiosk_currencies` serves the property's own **default accounting currency** (`Enterprise.Currencies[].IsDefault` from `configuration/get`) — THB for the six Thai properties, PHP for Makati, USD for Siem Reap. It renders as a plain label, not a dropdown, because there is one. Two earlier versions were wrong and are worth not repeating: a hardcoded THB/USD/PHP list (Marasca offered a PHP it does not use), then MEWS's `IsEnabled` list — which means *accepted for payment*, not *priced in*, and let Marasca/Koh Samui/Patong/Makati offer a USD they do not quote in. `IsDefault` matched all eight properties; `IsEnabled` matched four. `cashiers/getAll` is **not** the source and never can be — a MEWS Cashier object carries no currency field at all — and `exchangeRates/getAll` / `currencies/getAll` are both 401, so `configuration/get` is the only reachable source.
- `/admin/kiosks` (Admin Console > Kiosks) — The back-office settings behind the `/kiosk` screens, laid out to read the same way as MEWS's own Kiosk configuration form so the two can be compared field by field. One row per kiosk in `kiosk_settings` and **a property can have several** (MEWS keys them the same way, and a hotel really does run a 6AM lobby terminal and a late-night one with different grace periods and messages); which property's kiosks are listed comes from the app-wide switcher, not a dropdown of its own. Everything goes through `api/app/routers/kiosks.py` with the service role rather than straight from the browser, because the row holds `pin_code`, which unlocks the settings screen on a terminal standing in a public lobby — the PIN is minted with `secrets` on create, never typed. The `/kiosk` screens read this config through `GET /kiosks/config`, which is the **one endpoint that withholds `pin_code`** — the terminal asking for its own settings is the device standing in the lobby, so `_GUEST_SAFE_FIELDS` lists what it may know rather than the row being passed through. Without device pairing (removed — see `/kiosk`), a terminal takes its property's first kiosk by name; `?kiosk=<id>` on the URL points a specific terminal at a specific row, and that parameter is the seam real pairing would use. `src/app/kiosk/kioskConfig.tsx` fetches it once for the whole flow so a check-in can't change its mind halfway through, and **every screen still renders when the config is null** (no kiosk configured, or the fetch failed) by falling back to its built-in defaults — a terminal nobody has set up must still let a guest check in. What the config actually drives today: the welcome hero photo (first uploaded image), theme and default language on the four light-theme screens (below), what the search screen's box matches (per `reservation_lookup` — wired 22-Sep-2026; before that the setting was stored, offered in Admin and read by nobody, and the box always matched names only), the three texts on the success screen, and the idle screen saver video. **`theme` (Light/Dark) is applied, but only to Welcome/Search/Confirm/Registration** — those four read it through `data-kiosk-theme` and the `--kiosk-*` CSS variables in `globals.css`'s `.kiosk-scheme` block (`kiosk/layout.tsx`), not hardcoded hex classes, specifically so this field could stop being a no-op; eKYC/upsell/payment/success still hardcode their own dark palette directly in Tailwind classes (no reference screenshot yet for them) and ignore both `theme` and the language switcher below. `LANGUAGES` here now offers all four languages the front-end selector supports (`src/app/kiosk/i18n.ts`) — keep the two lists in step. **Options** (renamed from "Options enabled" 24-Sep-2026, alongside a redesign from pill buttons to MEWS-style toggle rows) is now a 10-item list, the first 9 copied field-for-field from a real MEWS "Edit kiosk > Options" screenshot (title, description and `key`/order all matched to it, since `key` is the literal string `options_enabled` stores) plus *Skip upsell*, which is NHGOne's own addition and not one of MEWS's real Options. Only two are actually read: /kiosk/registration's *Guests can remove other guests* gates the Remove button on a terminal-added guest, and *Skip upsell* sends Next to `/kiosk/payment` instead of `/kiosk/ekyc` (eKYC still runs — it is identity verification, not an offer). *Staff mode* is stored only, since there is no separate guest-mode view to switch away from, and the six new MEWS-sourced options (automatic space reallocation, company profiles, full-payment requirement, split bills, space upgrades, space selection, ID camera scan) are recorded only — none of the flow they'd govern exists yet, same as the Hardware/Reception fields elsewhere on this page. A fifth option, *Require signature*, was **removed** 22-Sep-2026 (before this list grew): Check In Form > General > Signature says the same thing per guest type and is the one actually read, and two controls for one behaviour is one too many. Grace periods, hardware, `early_checkin_fee`, `payment_method`, `cut_key_*`, `checkout_instructions` and **Reception** (below) are still stored and unread — the check-in, upsell and payment screens behind them are mock (and `reservations/start`, `products/getAll` and the payment operations are all still 401), so reading them would only dress up a screen that does nothing. Two deliberate departures from MEWS's form, both because pretending would be worse than the gap: there is **no "Add translation"** (we store one set of texts, so the control would do nothing — the language they belong to is shown above each instead), and **Key cutter / Key issuing / Payment terminal are free text, not dropdowns** (no device registry to populate a list from, and a dropdown would imply they are wired to hardware). Grace periods are clamped server-side (0–240h / 0–59m) — an unclamped 900 minutes would store and silently mean 15 hours. Their hints were corrected 24-Sep-2026 against the user's own description of what they mean at this property: **check-in grace is how long BEFORE the scheduled arrival time a guest may already check in**, and **check-out grace is how long AFTER the scheduled departure time one may still check out** - the check-in field's hint previously said "after," which was backwards. **Reception** (added the same day, against a real MEWS "Reception" screenshot) sits between Options and the grace periods and is what those two hints now name explicitly: `reception_arrival_hours/minutes` and `reception_departure_hours/minutes` (default 15:00/12:00, clamped server-side to 0–23h/0–59m — a TIME OF DAY, not a duration, so it gets its own clamp range distinct from the grace periods') record the property's standard scheduled arrival/departure that the grace periods above are relative to. "Bookable period" renders alongside them as a locked `Nights` field, matching the reference screenshot's own greyed-out control, and is never stored — there's nothing to save for a value that's always the same. Needs `api/sql/kiosk_reception_hours.sql`
- `/admin/kiosks/checkin-form` (Admin Console > Kiosks > **Check In Form**, shown indented under Kiosks in the sidebar — see `NavItem`'s `sub` flag in `Navigation.tsx`, a single visual nesting rather than a general collapsible-group mechanism) — which guest profile fields are collected during self-service check-in, laid out to read the same way as MEWS's own Check in form screen: a category of tabs (General / Address / Documents / Verification), each a table of fields × guest type (Reservation owner / Other adults / Children), each cell one of Default / Required / Optional / Hidden. One row per **property** in `checkin_form_settings`, not per kiosk device like `kiosk_settings` — guests fill out the same form regardless of which physical terminal they use. **Field-list fidelity**: `general`, `documents` and `verification` in `FIELD_CATEGORIES` (`src/lib/checkinFormFields.ts` — moved there when the kiosk started reading it, so the editor and the terminal share one list) are copied field-for-field from real MEWS screenshots, including which two cells are locked (Last name is always Required for every guest type; Relation to other guests is always Hidden for the reservation owner, who has no one to be "related to" on their own card). `address` was corrected 23-Sep-2026 against a real screenshot too: MEWS's form has **no Address line 2** (removed — the earlier list here was a placeholder guess, never verified, and this shipped as a real field on the kiosk form until the correction; a guest saved before it keeps whatever line 2 they typed, sitting unread in their encrypted blob), and its last three fields order as **Postal code, Country, State/Province** (not the near-alphabetical guess it shipped with). `state_province`'s existence and position are now confirmed; its exact field name is still unverified. **Documents also carries per-document field tables** (`DOCUMENT_FIELD_CATEGORIES`), added 23-Sep-2026 from the same reference screenshot: MEWS shows a separate Field × guest-type table for each document kind the chosen Type includes (`DOCUMENTS_ALLOWED[type]`), not one shared table — Passport and ID card are verified (each locks its document-number field Required for the owner, same reasoning as General > Last name; Passport's Issuing city defaults Hidden where ID card's defaults Optional — the one real difference between the two tables). Driver's license appears in the same screenshot but has no table yet. Stored under its own `documents_<kind>` key (`documentCategoryKey()`) so it can't collide with the plain `{type, visibility}` object under `documents` itself, and reuses `effectiveState()`/`FieldsTable` unchanged — `findField()` just also checks `DOCUMENT_FIELD_CATEGORIES` for a `documents_<kind>` category key. **Nothing reads these yet**: the kiosk still asks for one plain "document number" governed by `documentSettings().visibility` alone, so every row here shows *Not on the kiosk* — the configuration surface again going in ahead of the screen that would consume it. A locked cell renders as fixed text with an info tooltip instead of a dropdown, and is never stored — the UI computes it the same way every load, so there's nothing to save for it. The "Other adults"/"Children" column headers carry a checkbox that greys out the whole column (stored values are kept, not deleted, so re-enabling restores them) rather than being editable per guest type existing at all. **`/kiosk/registration` obeys this** (since 21-Sep-2026): a field set `Hidden` is not rendered at all, `Required` is starred and blocks Next, `Optional` does neither; unticking *Other adults* removes the kiosk's Add guest card. `effectiveState()` resolves a cell as locked → the property's saved choice → MEWS's own default → `kioskDefault` → Optional; `kioskDefault` exists so the admin table can keep showing the literal "Default" the reference screenshot shows while the terminal still decides something (First name and Signature resolve to Required). **The same resolution runs again server-side** (`api/app/routers/checkin_form.py`'s `load_fields`/`effective_state`/`document_settings`, used by `kiosks.add_kiosk_guest`), which both enforces the required set and **blanks** a Hidden field rather than merely not checking it — a property that chose not to collect an occupation must not end up storing one because a client posted it anyway. Keep the two copies of the defaults in step. The kiosk collects a **subset** of this table (`KIOSK_COLLECTED_FIELDS`); every other row is marked *Not on the kiosk* in the editor rather than silently doing nothing. Note the out-of-the-box consequence: MEWS's own defaults are Documents `Hidden` and Occupation `Hidden`, so a property that has never saved this page gets a kiosk form with no identity-document section — one Save on this page is what turns it on. `api/sql/checkin_form_settings.sql` adds the table (RLS on, no policies — backend-only, same shape as `kiosk_settings` though nothing here is as sensitive as its `pin_code`)
- `/osh-checklist` (sidebar **"OSH"**) — the Occupational Safety & Health inspection FORM, ported 24-Sep-2026 from a standalone React prototype (a Gemini Canvas app whose source was shared as a Google Doc) with its screens, texts, 136-item checklist, 6 categories and "NARAI Format" report template kept **verbatim** (`src/lib/osh/defaults.ts`, extracted by script). Originally a three-item sidebar group ("OSH Checklist" > OSH Form/Report/Setting); flattened the same day at the user's request so this single page IS the main-sidebar menu (renamed to plain "OSH"), with **Report and Setting moved to Admin Console > OSH** (below) — a front-line inspector's menu should hold the form they fill in, not the back-office pages that read every property's history or decide where reports get emailed. The prototype kept everything in `localStorage`; here every piece is shared state behind `api/app/routers/osh.py` (service role, all tables backend-only): settings in `osh_settings` (one row per prototype key — `properties`/`categories`/`checklist`/`emails`/`template`; a missing row means the built-in default), the in-progress form in `osh_drafts` **per signed-in user** (not per browser — a shared front-office PC no longer greets one inspector with another's half-finished form), submitted reports in `osh_reports` (a snapshot of header + items + score). The three pages' shared pieces (`oshClient.ts`'s hooks, `Toast.tsx`, `SetupBanner.tsx`, `ImageCropperModal.tsx`) live in `src/lib/osh/` (moved there the same day, since Report/Setting now live in a different route tree entirely and a relative `../` import can no longer reach them). Photos are uploaded to the public `osh-photos` bucket the moment they are attached (random uuid paths, created by the backend on first upload) and items hold the URL, never base64 — at up to 136 photos a report, inline images are what made `bcp_snapshots` the biggest table here. **Confirm & Submit really emails** the property's recipients from Admin Console > OSH > Setting > Email (read server-side from `osh_settings`, never from the request, so a client can't redirect a report) with the report attached as a standalone HTML file (opens in any browser, prints to A4 PDF) and a link to `/admin/osh/report?id=<id>`; the prototype only showed a toast claiming it had. Outcome (`sent` / `not_configured` / `failed`) is stored on the row, and a mail failure never fails the submission. Report rendering (`src/lib/osh/report.ts`'s `renderReportHTML`) is the one function behind View Custom PDF, Setting's live preview and the emailed file; like the prototype, a filed report renders through the **current** template. Prototype bugs fixed on the way (each commented where fixed): its status/sub-status substitution had lost its `${}` and emitted broken markup; every value is now HTML-escaped (the template goes through `dangerouslySetInnerHTML` and into email); a new checklist item's code was literally `{001}` (now `A023`-style); Insert Variables listed a broken `{A001}_SubStatus` and omitted `_Status`/`_Photo`, and "click to insert" was never wired; the template Preview only ever showed an "upload Excel" placeholder with no upload behind it (now a live render with the prototype's mockup data); deleting a category orphaned its items (now blocked while it has items). Two permissions, and NOT a Form/Report-vs-Setting split: `role_permissions.osh_checklist` gates **only this page** (the front-end OSH Form) — whoever actually fills it in. `osh_settings` gates **the entire back-office OSH area in Admin Console, Report AND Setting together** (a "P&C"-style role reviewing every property's filed reports and configuring the checklist/recipients, distinct from who may fill the form at all). Corrected 24-Sep-2026: an earlier version put Report under `osh_checklist` alongside the Form, which didn't match how the module is actually staffed — a role that fills the form has no particular need to read every property's history back. Enforced two ways: this page by `Navigation.tsx`'s ordinary sidebar route guard (`osh_checklist` alone), the two Admin sub-pages by the same `limitedAdminPaths` mechanism RR4-Nationality/TM30-Nationality and Email Template use (`ADMIN_OSH_PATHS`, gated on `osh_settings` alone) — a role with `osh_settings` but not full `admin` reaches both Admin Console sub-pages without the rest of the console. **Submitted Reports History shows who submitted** (`submitted_by_email`, added 24-Sep-2026 alongside the existing display-name `submitted_by`) - the signed-in account's email, not just their name, so a report can be traced back to an account rather than a free-typed Operator field. Submitting before the incremental `alter table ... add column if not exists submitted_by_email` has run still succeeds (`osh.py`'s `_missing_column` catches PostgREST's schema-cache miss and retries the insert without that field) - the report is what matters, not the one extra column. Property list = the OSH list narrowed by `getAllowedProperties()` for a restricted role (Form's picker and the Report page's reports alike); Makati is spelled "Lub d Philippines Makati" (the prototype's "Lub d Manila Makati" would never match a restricted role). Needs `api/sql/osh_checklist.sql`; until it runs every page opens on the defaults with a banner, and anything that saves reports that the SQL is missing
- `/admin/*` — Dashboard, User Management, Email SMTP, Sync & Schedule (per-feature schedules, Retry Policy, FTP Upload), Property & API (per-property tokens, `st_property_code`, RR4/TM30 window), Email Template, Kiosks, **OSH** (Report + Setting — see `/osh-checklist` above for the rest of the module), RR4-Nationality, TM30-Nationality, Activity Log

### Emails and FTP delivery

`api/app/services/email_service.py` sends through the single global `smtp_settings` row. Templates live in `email_templates` keyed by `template_key`, edited at **Admin > Email Template**, whose tabs are grouped (`GROUP_CONFIG` in `src/app/admin/templates/page.tsx`): Billing, RR3, **System Email** (welcome / internal welcome / password reset / Google sign-in notice / approved / the ST and RR4/TM30 verification mails below), **Statistic Files** (bundled digest + per-property), **RR4 / TM30 Files** (same pair), **Revenue** (the stop-sale notification below), **RV Files** (the RV verification mail below). Grouping exists so the pill row doesn't grow one more top-level tab per template; a group with a single child renders as a plain tab.

Each digest has both an **All Property** bundled form and a **Per-Property** form on that property's own send time, and a `_last_sent_date` marker column so a repeated cron tick can't re-send. Save and "Send Test Now" are deliberately **two separate buttons** — don't merge them.

### Sheet-verification mails (temporary monitoring)

Three daily mails compare what NHGOne produces against the Google Sheets that are the **ground truth** for what gets filed, so the new system can be watched before it replaces them. ST and RR4/TM30 live at **Admin > Email Template > System Email** (Test ST File / Test RR4/TM30 File) and RV at **Admin > Email Template > RV Files** (Test RV File), each with its own recipients, send time (RR4/TM30 and RV 08:00; **ST 02:30** — see below), subject and body — and all three are removable without SQL, since `email_templates` already has every column they use.

- **`st_compare_service.py`** — ST Files vs each property's `<Name>-ST` sheet, 9 metrics × 8 properties. All 8 sheets must agree on one date or nothing is sent; they hold one pasted export each, so a day they no longer hold cannot be reconstructed. **The send time never changes what is compared** — by explicit requirement, whatever time the mail is set to within the day it must compare the same sweep. Two mechanisms make that true:
  - **It compares the date's scheduled SWEEP, not the latest import.** `sync_st_files_day(scheduled=True)` (the daily run and its retries) records its import as the sweep (`data.sweep_synced_at`); a manual Import To Data Mart still refreshes `data.blob` — what the ST Files page, the export file, FTP and this mail's CSV attachments read — but carries the sweep aside as `data.sweep_blob` instead of overwriting it (`sync_service.st_sweep_of` resolves both shapes). A row with no sweep on record (imported before 17-Sep-2026, or only ever by hand) falls back to its latest import and the Sweep Time table labels it so. `scripts/st_compare.py --latest` compares the latest import instead, for checking a fix right after a manual re-import.
  - **A scheduled send reports each sheet date exactly once, keyed to the sweep rather than the clock** (`compare_mail._st_not_ready`). It waits for the next cron tick, without marking the day sent, while the sheets still hold a date this feed already reported (read back from the last `auto` success row in `sync_logs`, whose message starts `<date>:`) or while any property's import of the date isn't in yet. A clock rule ("the sheets must hold yesterday") was tried first and rejected: it breaks any send time earlier than that morning's sheet exports. Send Test Now and the CLI skip the guard.
  - Currently set to **02:30**, right behind the last sheet exports (~02:21) and the imports that mirror their times. The `<<SweepTable>>` token shows **when each side pulled from MEWS**: the sheet's `Created` stamps from its hidden `Parameters` tabs (Availability export, plus the Reservation export where the sheet pastes one) against our `synced_at`, in each property's own timezone (MEWS stamps exports in enterprise-local time — Makati's 01:18 is Manila, lined up with our 00:19 Bangkok import), with the gap flagged past `SWEEP_GAP_WARN_MINUTES` (30). A flagged gap explains Occupied/Availability/Customers differences on its own: those are read live, so a booking made between the two pulls shows up as a difference neither side got wrong.
- **`rr4_compare_service.py`** — RR4/TM30 vs each Thai property's `RR4-TM30-<Name>-Gen` sheet, Thailand only (Siem Reap and Makati have no generator sheet and never will). Rows are paired by passport/PID and then compared **column by column** — a key-based diff that stops at "found a row with this passport" once called Patong's TM30 a perfect match while a full-field diff surfaced 6 differing rows. Unlike ST, **each property is compared at its own sheet's date**: Chinatown cuts its day at 12:15 where the rest cut at ~02:00, so it always runs a day behind and one shared date would mean never sending. Four documented drift patterns (`_KNOWN_DRIFT`) are counted separately from real differences so the headline number stays meaningful. It also reports each sheet's own export window against `property_api_settings.rr4_tm30_day_start_*`, since the sheets change theirs without warning and a stale value silently undercounts the register.
- **`rv_compare_service.py`** — RV Files vs the single **RV** Google Sheet, one tab per property (tab names `Chinatown`, `Siam`, `Samui`, `Kohtao`, `Makati`, `Patong`, `Siemreap`, `MRCSamui`), each holding that property's PMSRV file pasted in as text lines — so the comparison is **line by line**, there being no total to read. Lives at **Admin > Email Template > RV Files > Test RV File**, not System Email. Three rules keep its headline honest: a pasted line that split across two cells at a comma is rejoined with `,` (Patong); amounts compare as numbers (the sheet holds `1700`, we write `1700.00`); and a description that differs only by `×` → `x` is **known drift** — the sheet's own paste/typing changed it, **not** MEWS: MEWS's real PMSRV export is plain ASCII (`x`), matching ours exactly, confirmed against a real file (`sync_service._ascii_fold`'s own docstring). `get_rv_export` deliberately runs `_ascii_fold` (one non-ASCII byte makes SunSystems reject the whole day) for the same reason MEWS's own file is ASCII. Leftover lines are paired on account + D/C + amount, then diffed field by field. Each tab is compared at the date its own lines carry, against the cached `rv_files_sync` import. Measured 13-Sep-2026: under those rules all 947 lines across 8 properties matched, except 28 `×` descriptions in the sheet.

`compare_mail.py` is the single send path all three use — the scheduled job, the Admin "Send Test Now" button and the three CLIs (`scripts/st_compare.py`, `scripts/rr4_compare.py`, `scripts/rv_compare.py`) all go through it, so a test send and the real one can't disagree. A comparison that isn't in a usable state logs and sends **nothing**: a mail reading "ตรงกับชีตทั้งหมด" built from an empty comparison is worse than no mail.

### Stop-sale notification

`stop_sale_alert_service.py` — the daily "what newly stopped selling, or re-opened" mail off /revenue's Occupancy By Type Calendar (**Admin > Email Template > System Email > Notification - Revenue New Stop Sale and Re-open**, 09:00 Asia/Bangkok by default). Same `email_templates`-row-is-the-whole-config shape as the two verification mails above, so it needs no migration either, and `compare_mail.due` is the shared schedule gate.

It reads **`occupancy_sync` only** — no MEWS call — diffing each property's two newest snapshots exactly the way the calendar diffs the on-screen snapshot against its baseline, and reporting only the nights whose state *flipped*: an existing stop is not news. Three things it does deliberately differently from the calendar:

- **A missing occupancy figure on either side is skipped, not treated as "not stopped".** The calendar renders a missing value as un-stopped, so a gap today against a stop yesterday draws as a re-open; in an alert that is a false one.
- **A night or category the baseline never covered is skipped.** Snapshots are anchored to whole months a year forward, so the newest one always reaches a month further than its predecessor — nothing there can be called *new*.
- **"Nothing changed" IS sent** (unlike the compare mails), because a daily all-clear is the point of a watch. What is never sent is a mail built when *no* property has two snapshots — with no earlier position, an all-clear would be a lie.

The threshold is a constant (`DEFAULT_THRESHOLD = 90`) mirroring `DEFAULT_STOP_SELL_THRESHOLD` in `src/app/revenue/page.tsx`: the on-screen field is per-session and never persisted (it is PIN-gated precisely because it changes the business definition), so there is no user value to read — the mail names the number it used instead. Occupancy now captures twice a day (see below), so "the two newest snapshots" it diffs can be same-day (13:00 vs 08:00) or cross-day depending on this mail's own send time — either way it is always "since the last capture."

`ftp_service.py` uploads the ST and/or RV export files to one global plain-FTP destination (`ftp_settings`, Admin > Sync > FTP Upload), on its own `upload_hour`/`upload_minute`, with independent `upload_st_files` / `upload_rv_files` checkboxes. One destination folder serves every property because the filenames already disambiguate (`MS_ST_20260808.csv`, `MS_RV_20260808.csv`). Like `get_ftp_settings`, several of these config readers **degrade to defaults if the table doesn't exist yet** rather than raising — a deliberate pattern so a feature keeps working before anyone has touched Admin.

### Ground truth

The statutory and finance reports are ports of spreadsheets that are already being filed. **The per-property Google Sheets are the ground truth for RR4/TM30, and the ST Master sheets for ST Files** — our output must match them, and any difference is investigated as ours until proven otherwise. Several rules in the code look wrong until you know they were copied deliberately: RR4's `occupation`/`willGo`/`willGoCountry`/`timeCheckOut` are fixed constants in the source sheet, not derived per guest; the ST file's field 5 is a report-period month code, not a property code; the nationality tables preserve corrections for genuine typos in the source sheet (Greece's row containing Greenland's data, North Korea's row mislabeled). Comparison is column-by-column — some recurring diffs are live drift in MEWS, not bugs.

For the SunSystems RV/ST exports, the scope boundary is: **correct format and correct numbers**. Finance-side configuration gaps are theirs, not ours to chase.

### Auth guard

`src/components/Navigation.tsx` wraps every page. It checks Supabase Auth and verifies the user has a row in the `profiles` table. `/` and `/reset-password` are the only public routes — the latter is skipped by the guard entirely (`isResetPasswordPage`) because its visitor arrives holding a one-time recovery token rather than a session, and the page validates that itself. Both profile lookups in the guard use `select("*")` rather than a column list, for the same reason `role_permissions` does: a column that exists in code but not yet in the database would otherwise fail the whole query and sign *everyone* out as unauthorized.

New signups (mainly first-time Google OAuth logins) are auto-provisioned by a Postgres trigger on `auth.users` (`on_auth_user_created` → `handle_new_user()`, managed directly in Supabase SQL Editor, not a tracked migration) as `role='User'`, `status='Pending'`, with `full_name` defaulting to the email's capitalized local-part unless Google supplied a real name. A `Pending` profile sees a "Waiting for Approval" screen instead of the app shell/menus. A Super Admin approves via Admin > Users, picking a real role — this calls `POST /admin/users/{id}/approve`, which sets `status='Active'`. A still-missing profile (trigger somehow didn't fire) falls back to `POST /admin/self-register` (same fixed `role='User'/status='Pending'` defaults) before the old sign-out-as-unauthorized path. Admin-created invites (`POST /admin/users`) are unaffected — that endpoint's own upsert overwrites the trigger's defaults with the real role and `status='Active'` immediately after creating the auth user. If that upsert fails, the just-created auth user is deleted again rather than left orphaned: an auth row with no profile takes the address hostage (retrying the same email fails as "already registered" while the person still can't sign in).

**Google vs Internal accounts.** Admin > Users' create dialog has a *User Authentication* choice (`profiles.auth_method`). `google` (the default) generates a throwaway random password purely so the Supabase Auth row exists — the real credential is Google OAuth, linked by email. `internal` generates a *real* password, emails it to the user (`send_internal_welcome_email`, hardcoded rather than an Admin > Email Template design so a stray template edit can't drop the password and mail an unusable account), and sets `profiles.must_change_password`. That flag makes `Navigation.tsx` render `ForcePasswordChangeScreen` instead of the app shell — same blocking-screen pattern as `Pending` — until the user replaces the emailed password. Clearing the flag is client-side, consistent with the rest of this app's `role_permissions`-style gating; it is a "don't keep using a password that sat in a mailbox" gate, not a security boundary.

**Forgot password** (Internal Auth only) is `POST /auth/forgot-password` in `api/app/routers/auth.py` — public, no session, deliberately not under `/admin`. It mints a Supabase recovery link with the admin API's `generate_link` rather than the client-side `resetPasswordForEmail`, so the mail goes out through this app's own SMTP and branding; the link lands on `/reset-password`. Every outcome returns an identical response — a form that answers differently for a real address than an unknown one is an account-enumeration oracle on a public login page — so the three real outcomes are invisible to the caller: internal account → reset link, Google account → a "sign in with Google instead" note to the mailbox's real owner, unknown address → nothing sent.

Which sidebar menus each role can see is controlled by the `role_permissions` table (one row per role, one boolean column per menu), edited via Admin > Users > Role Settings tab. The current columns are `dashboard`, `data_mart`, `bills`, `rr3`, `st_files`, `revenue`, `rv`, `bcp`, `rr4_tm30`, `reconciliation`, `users_report`, `kiosk`, `osh_checklist`, `osh_settings`, `admin` — mirrored in the `MenuPermissions` interface in `src/lib/menuPermissions.ts`, which any page can call to see the same set the sidebar would show. **Adding a menu means adding a column here, a field there, an entry in both fallbacks, and an entry (with its `lucide-react` icon) in `navEntries` in `Navigation.tsx`.** That one list feeds both the desktop sidebar and the mobile drawer. The desktop sidebar collapses to an icon-only rail from the hamburger at its top (MEWS-style, name shown as a tooltip), remembered per browser under `localStorage["nhgone.sidebarCollapsed"]`.

**The rest of the shell is MEWS-shaped too, and two pieces moved there deliberately:**
- **The signed-in user lives at the bottom of the sidebar**, not in the page header — `UserHeader.tsx` exports `ProfileMenu` (variant `sidebar` / `topbar`) and `ThemeToggle` separately for exactly that. Collapsed, it shrinks to the avatar with the name as a tooltip, and its menu opens upward/right. The sidebar therefore keeps `overflow-visible` and scrolls only its **link list**, or that menu and the rail's tooltips would be clipped by it.
- **One property for the whole app**, picked from `PropertySwitcher` at the top-right of every `PageHeader` (and, compact, in the mobile top bar) instead of a per-page "Select Property" dropdown — there is no longer one on any page. The state is `src/lib/propertyContext.tsx` (`useSelectedProperty()`), mounted by `Navigation.tsx` **inside the signed-in shell** so its list is fetched with a session and after the role is known, and remembered under `localStorage["nhgone.selectedProperty"]`. It still resolves through `getAllowedProperties()`, so a property-restricted role can only ever switch within its own properties — which also closed a real gap, since Data Mart used to query `property_api_settings` directly and ignore that restriction. The switcher reads **only** `property_name` + the two image columns from that table, never `select("*")`: it also holds the encrypted MEWS tokens.

`Navigation.tsx` reads the row with `select("*")` (not an explicit column list) on purpose — if a newly added menu column doesn't exist in the DB yet, an explicit list would fail the whole query and dump every role onto the hardcoded fallback; with `*` the missing column just hides that one link. Roles are not a fixed set — the Role Settings tab has an "+ Add Role" input that inserts a new `role_permissions` row (Dashboard-only by default), and that role immediately becomes selectable in the Create/Edit/Approve user role dropdowns, which all read from this table rather than a hardcoded list. Super Admin is the only row that can't be edited/duplicated via the grid. `Navigation.tsx` and `UserHeader.tsx`'s Admin Console link both read this table for the signed-in user's role; Super Admin is hardcoded to always pass regardless of the table. A missing/unloaded row falls back to the old hardcoded rule (Finance = Bills only, everyone else = full menu) so a bad row can never zero out a user's menu entirely. `role_permissions` has RLS **enabled** with an `authenticated`-only policy so the browser's signed-in client can still read/write it — note the failure mode if you ever add a table here: RLS on with *no* policy silently returns empty results to the frontend rather than an error, which for this table would dump every role onto the hardcoded fallback. See **Row Level Security** below.

`role_permissions.restricted_properties` (a text array) locks a role to a subset of properties: empty/NULL (the default, and Super Admin always regardless) means unrestricted — sees every property. List one or more `property_api_settings.property_name` values and the app-wide property switcher resolves through `src/lib/allowedProperties.ts`'s `getAllowedProperties()`, which returns just those instead of the full list — used for property-level staff (e.g. a "Lub d Bangkok Siam Front Office" role) who should never be able to pick another hotel out of the dropdown. This is **client-side gating only** (matching the rest of the app's `role_permissions` security model, e.g. the admin route guard) — it is not enforced at the FastAPI layer, so it does not stop a user calling the API directly with a different `property_name`.

`Navigation.tsx` also enforces `/admin/*` route access itself (not just hiding the sidebar link): Super Admin always passes; every other role needs `role_permissions.admin === true` or gets redirected to `/dashboard`.

### Key Supabase tables

| Table | Purpose |
|-------|---------|
| `reservations_sync` | Encrypted reservation snapshots; `mews_id` is the unique key |
| `members_sync` | Encrypted member snapshots; `mews_id` is the unique key |
| `payments` | Payment records; `mews_id` is the unique key |
| `resources_sync` | Encrypted MEWS resource (room/space) snapshots; `mews_id` is the unique key |
| `bills_sync` | Archived MEWS bill headers + order items + owner address/tax ID; `mews_id` is the unique key; backs the Bills page's "Database" mode and `get_bill_invoice`'s cache path |
| `property_api_settings` | Per-property MEWS tokens (encrypted), `st_property_code`, the RR4/TM30 day window, `profile_image_url` + `background_image_url` (the switcher's logo and banner, set at Admin > Property & API > Edit), and **four independent schedules** (`sync_*`, `st_files_sync_*`, `occupancy_sync_*`, `rr4_tm30_sync_*`, `rv_sync_*`) plus per-property email send times |
| `profiles` | Authorized users; required for login. `status`: `Pending` (awaiting Super Admin approval) → `Active`. `auth_method`: `google` (default) or `internal`; `must_change_password`: set on internal accounts until they replace the emailed password |
| `role_permissions` | Role × sidebar-menu access grid (one boolean column per menu, one row per role) + `restricted_properties`, edited at Admin > Users > Role Settings. Adding a menu = a column here, a field in `MenuPermissions` (both `Navigation.tsx` and `src/lib/menuPermissions.ts`), an entry in `MENU_ITEMS` and in `handleAddRole`'s default row (`admin/users/page.tsx`), and a decision about both fallbacks |
| `sync_logs` | Per-sync result log (`sync_type`: `auto` / `manual` / `retry`) |
| `sync_locks` | DB-level mutex to prevent concurrent syncs |
| `sync_retry_settings` | Retry Policy (count + interval in minutes) for `retry_scheduled_syncs` |
| `smtp_settings` | Single global SMTP config (encrypted password) for system emails |
| `ftp_settings` | Single global FTP destination (encrypted password) + upload time + per-report-type checkboxes |
| `email_templates` | All HTML email bodies keyed by `template_key`, edited at Admin > Email Template |
| `billing_templates` | Per-property HTML invoice/receipt template (`<<Token>>` placeholders), Admin > Email Template (Billing tab) |
| `rr3_templates` | Shared HTML template for the ร.ร.๓ lodger registration card (`<<Token>>`), Admin > Email Template (RR3 tab) |
| `st_files_sync` | Cached ST Files daily reports, one row per (property, report_date), whole report Fernet-encrypted as `data.blob` (latest import). The scheduled import also marks `data.sweep_synced_at`; a later manual re-import moves that sweep aside to `data.sweep_blob` rather than overwriting it — the ST verification mail compares the sweep (`sync_service.st_sweep_of`) |
| `rv_files_sync` | Cached RV revenue journals, same one-row-per-(property, date) encrypted-blob shape |
| `rr4_tm30_sync` | Cached RR4 **and** TM30 for one day in a single row (`{"rr4": ..., "tm30": ...}`), encrypted blob |
| `rr4_tm30_overrides` | **Durable, never overwritten by a re-import** — manual per-guest corrections to a register, one encrypted row per edited guest, applied over the raw report on every read |
| `occupancy_sync` | Occupancy + Rate snapshots, one row per actual capture (up to 2/day - 08:00 and 13:00); plain `jsonb` (no PII), newest 6 per property |
| `rv_gl_mappings` | Per-property GL account/department per MEWS `AccountingCategoryId`, pulled from `accountingCategories/getAll`; checked before the hardcoded `_RV_CHARTS` |
| `kiosk_settings` | One row per kiosk (a property may have several), edited at Admin Console > Kiosks. **Backend-only** — RLS on with no policies, stricter than `property_api_settings`, because the row carries `pin_code` |
| `kiosk_reservations_sync` | Per-minute mirror of each enabled property's arrivals for **today** (every MEWS state; the kiosk offers only `Confirmed`), one row per `mews_id`, `guest_name`/`guest_email` Fernet-encrypted per field. Earlier days are deleted on every run. Which properties are mirrored is `property_api_settings.kiosk_arrivals_sync_enabled` (Marasca Samui only, via `api/sql/kiosk_reservations_sync.sql`). **Backend-only** — RLS on with no policies |
| `checkin_form_settings` | One row per **property** (not per kiosk), edited at Admin Console > Kiosks > Check In Form. **Backend-only** — RLS on with no policies |
| `osh_settings`, `osh_drafts`, `osh_reports` | OSH Checklist: settings (one row per key, missing = built-in default), one draft per user, submitted report snapshots with their email outcome. **Backend-only** — RLS on with no policies |
| `rr4_nationality_codes`, `tm30_nationality_codes` | Editable alpha-2 → form-code nationality maps, layered over `rr4_tm30_reference.py` |
| `bcp_snapshots` | 5-minute BCP snapshots, gzipped + encrypted as `data.blob`; pruned to the newest 12 per property |
| `bcp_action_logs`, `bcp_reg_cards`, `bcp_reservation_notes`, `bcp_room_changes`, `bcp_*_overrides` | **Durable, never pruned** — everything front desk enters during a BCP outage |

Storage buckets: `avatars` (profile pictures, read via `getPublicUrl`, uploaded straight from the browser) and `property-images` (each property's logo + banner for the switcher, `api/sql/property_images.sql`). Buckets are **not** covered by the table RLS pass below, so the two differ on purpose: `property-images` has **no storage policies at all** and is written only by the backend's `POST/DELETE /admin/sync/properties/{id}/image` with the service role, so no browser can write to it. That bucket is **created by the backend on the first upload if it is missing** (`_ensure_property_image_bucket`), so only the two *columns* still require running the SQL — the old behaviour was to fail the upload telling whoever was standing in Admin to go and run SQL, which is a dead end. Both uploads go through **`src/components/ImageCropDialog.tsx`** first: the Google-account-style cropper (fixed crop box, drag to reposition, zoom slider) whose canvas output — not the file the user picked — is what gets stored, so what was framed is what is saved. A logo/avatar crops square with a round mask, a banner 3:1, a kiosk image 16:9. A third bucket, `kiosk-images`, holds the Kiosks page's gallery on the same terms, and a fourth, `osh-photos`, OSH Checklist's evidence photos (uploaded through `POST /osh/photos`, uuid-named, never overwritten); `api/app/services/storage_buckets.py:ensure_public_bucket` is the shared create-if-missing helper both use.

**No `alert()` or `confirm()` in the frontend.** Errors and confirmations belong in the page — an inline banner beside the control, or a dialog styled like the app. A native browser dialog reads as though the app broke, and the user has rejected them outright. Converted so far: `admin/api-settings` and `profile`. Roughly 90 calls remain across the other pages; convert a flow's popups as part of any work that touches that flow.

### Row Level Security

Every table in `public` has RLS enabled (2026-08-23; Supabase had flagged 17 tables with it off, 21 linter errors). This matters because the anon key is **public** — it ships inside the browser bundle — so "anon can read" means "the internet can read". The backend is unaffected throughout: FastAPI connects with `SUPABASE_SERVICE_ROLE_KEY`, which bypasses RLS entirely.

Two shapes are in use:

- **Backend-only tables** (all `bcp_*` override/log tables, `email_templates`, `bills_sync`, `smtp_settings`, `ftp_settings`, `st_files_sync`, `rv_files_sync`, `rr4_tm30_sync`, `rr4_tm30_overrides`, `occupancy_sync`, …) — RLS on, **no policies at all**. Nothing but the service role can touch them. The `rls_enabled_no_policy` INFO lint this produces is expected and correct, not a gap to close.
- **Browser-facing tables** (`profiles`, `property_api_settings`, `sync_logs`, `role_permissions`, `rr4_nationality_codes`, `tm30_nationality_codes`) — policies scoped `TO authenticated`. Every frontend call to these happens after `supabase.auth.getUser()`, so requiring a session changes nothing for real users.

**Any new table gets an explicit RLS decision, stated in the SQL.** Supabase's default is RLS *off*, which means anon-key readable, which means public.

`profiles` and `property_api_settings` additionally carry a `block_anon` **RESTRICTIVE** policy (`TO anon USING (false)`). Restrictive policies are AND-ed with the permissive ones and apply only to the roles they name, so this shuts anon out without disturbing `authenticated`. It was used instead of deleting the legacy permissive policies those two tables still carry — `profiles`' "viewable by everyone" and `property_api_settings`' misleadingly-named "Enable all for service role", which is really `ALL / public / true`. **Those legacy policies are inert only because `block_anon` outranks them; drop `block_anon` and both tables are wide open again.**

Two traps live in `profiles` specifically, both real and both previously latent:
- A "Super Admins can update any profile" policy whose `USING` clause selects from `profiles` itself. It does not currently recurse (the SELECT policy it re-enters is a plain `true`), but any future self-referencing SELECT policy will trigger `infinite recursion detected in policy for relation profiles`.
- Its own-row rule, `USING (auth.uid() = id)`, is **not** sufficient for updates. `Navigation.tsx` repairs a first-time Google sign-in by updating the row **by email** while its `id` still differs from `auth.uid()`, and `USING` is evaluated against the OLD row. The added `authenticated_update` policy (`USING (true)`) is what keeps that path working — tightening it back to own-row-only will lock new Google users out.

Known residual: the `authenticated` policies are `USING (true)`, so any signed-in user can read/write these tables. That matches the pre-RLS behaviour exactly (nothing regressed), but it does not stop one user editing another's role. Tightening it needs a real signed-in login test first, not just a SQL-side check.

This app holds passports, national ID numbers, guest addresses and per-property MEWS tokens. Raise security concerns proactively, and never widen access to make something work.

### Chunked upsert pattern

Supabase enforces a ~60 s statement timeout, so bulk writes are chunked. The reference implementation is the `chunked_upsert` closure in `daily_auto_sync` (`api/app/main.py`): it upserts in batches of 200 and, on a timeout error, retries that batch at half size (100). The routers (`members.py`, `payments.py`, `reservations.py`) do simpler inline chunked `.upsert()` calls (chunks of 200-500) without the retry step. Follow the `chunked_upsert` pattern — including the timeout retry — for any new bulk writes.

### MEWS API gotchas worth knowing before you debug

- **Paging is not optional.** `orderItems`/`payments`/`outletItems`/`accountingCategories` all page by `Cursor`; a missing pager looks exactly like missing data. `sync_service._rv_fetch_paged` is the helper.
- **Interval limits.** `services/getAvailability/2024-01-22` rejects an interval of 100 days or more ("The interval must not exceed 100D"); reservation windows are split at 89 days by `_split_date_windows`. Reservation-ID batches cap at 1000 per request.
- **There are three start times, and the un-versioned `StartUtc` is not the booked one.** `ScheduledStartUtc` (2023-06-06 endpoint) is what the guest booked and never changes. `ActualStartUtc` (same endpoint) is the real check-in. The un-versioned `StartUtc` is neither reliably: it stays at the booked time for a **late** arrival but **moves to the real check-in for an early one** (Patong #191808: booked 10-Sep 14:00, checked in 00:57, `StartUtc` reads 00:57). Use `ActualStartUtc` for anything a human would call a check-in time (a real bug in both RR3 and RR4), and `ScheduledStartUtc` for which *day* a booking belongs to (TM30). ST Files' Arrivals still read the un-versioned `StartUtc` — it matched its sheets on 16-Sep-2026 because none of that day's disputed day-use stays were early check-ins, so if ST Arrivals drifts on a day that has one, this is the first thing to test.
- **Time-unit boundaries are property-local midnight**, not UTC midnight, or MEWS answers "is not start of TimeUnit". Every report resolves the property's own timezone first (`_resolve_property_timezone`).
- **Connector permissions are per-property and can be missing.** Resource Categories 401s if not enabled; `serviceOrderNotes/getAll` is permission-gated and degrades to empty notes rather than failing the snapshot.

### Git workflow

Commit **and push to `origin main`** after every completed and verified change, without waiting to be asked each time. Commit messages in this repo follow `type(scope): summary` with the scope naming the module (`fix(rv):`, `feat(revenue):`, `security(rls):`, `docs(tm30):`).
